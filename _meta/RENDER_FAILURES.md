# Render failures

Icons that failed to render (malformed source SVG, etc.) and were skipped.

## full run

- `million` (software, svgl/million.svg): ("cairo returned CAIRO_STATUS_NO_MEMORY: b'out of memory'", 1)
- `svgl` (software, svgl/svgl.svg): could not convert string to float: '100%'
- `webcomponents` (software, svgl/webcomponents.svg): invalid literal for int() with base 16: 'r'
